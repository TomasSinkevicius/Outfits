@model IEnumerable<Outfits.Models.Recommendation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OutfitPostId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Recommendation1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Recommendation2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Recommendation3)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OutfitPostId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Recommendation1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Recommendation2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Recommendation3)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
